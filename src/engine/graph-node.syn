"use wrapped";

import * as intrinsics from './intrinsics'
import type { GraphNode } from './lowering2'

const Types = intrinsics.Types

export function makeNode(
    op: string,
    argIds: number[],
    argConsts: Float64Array,
    T: intrinsics.Types = Types.u64,
    U: intrinsics.Types = Types.u64,
    types: intrinsics.Types[] | undefined = undefined,
    label: string | undefined = undefined,
): GraphNode {
    return { op, argIds, argConsts, T, U, types, label, epoch: 0 }
}

export function copyNode(n: GraphNode, argIds = n.argIds, argConsts = n.argConsts, op = n.op): GraphNode {
    return makeNode(op, argIds, argConsts, n.T, n.U, n.types, n.label)
}
