type X<T> = T extends (x: any) => x is infer U ? U : never
type Y = X<(x: any) => x is boolean>
export const x: Y = true

export function f1() {
    type Z = ((x: any) => boolean) extends ((x: any) => x is number) ? boolean : number
    const x: Z = true // err
    const x2: Z = 1 // OK
    return x
}