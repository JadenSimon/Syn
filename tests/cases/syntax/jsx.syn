// JSX is OK inside .syn
const div = <div></div>

// single capital letters are _always_ parsed as type args, not JSX
function f() {
    // FIXME: the language server should not auto-complete for JSX tags here
    return <A>(x: A) => x
}