// @run: true

function assert(cond: boolean) {
    if (!cond) throw "Assertion failed"
} 

enum Test {
    one = 1,
    two = one+1,
    four = two << 1,
}
console.log(Test)

assert(Test.one === 1)
assert(Test.two === 2)
assert(Test.four === 4)
