interface Base {
  n: string | number
}

interface X extends Base {
  n: string
}

interface Y extends Base {
  n: number
}

type U = X | Y

export function f(x: U) {
    if (x.n is number) {}
    return x
}
