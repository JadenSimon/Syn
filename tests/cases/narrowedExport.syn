// @filename: one.syn
export function f(v: number | undefined) {
    if (v is number) return v

    throw `not a number: ${v}`
}

// @filename: two.syn
import * as one from './one'

export const x = one.f(1)